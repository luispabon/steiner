# Context Management Note

steiner's current context handling is the baseline pipeline used by the main agent and delegated child agents: system preamble, AGENTS.md conventions, project context, skills, active conversation history, tool results, and the normal retention and compaction path.

This note is intentionally brief. The removed smart-context experiment is no longer current guidance.
